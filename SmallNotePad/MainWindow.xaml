<Window x:Class="SmallNotePad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmallNotePad"
        mc:Ignorable="d"
        Title="Untitled - SmallNotepad" Height="600" Width="900"
        Closing="Window_Closing"
        x:Name="MainWindowElement">
    <DockPanel x:Name="MainDockPanel">
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top" x:Name="MainMenu">
            <!-- File Menu -->
            <MenuItem Header="_File">
                <MenuItem Header="_Open" Click="MenuFileOpen_Click" InputGestureText="Ctrl+O"/>
                <MenuItem Header="_Save" Click="MenuFileSave_Click" InputGestureText="Ctrl+S"/>
                <MenuItem Header="Save _As..." Click="MenuFileSaveAs_Click" InputGestureText="Ctrl+Shift+S"/>
                <MenuItem Header="_Close" Click="MenuFileClose_Click" InputGestureText="Ctrl+W"/>
                <Separator/>
                <MenuItem Header="_Recent Files" x:Name="RecentFilesMenu"/>
                <Separator/>
                <MenuItem Header="E_xit" Click="MenuFileExit_Click" InputGestureText="Alt+F4"/>
            </MenuItem>

            <!-- Edit Menu -->
            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo" Click="MenuEditUndo_Click" InputGestureText="Ctrl+Z"/>
                <Separator/>
                <MenuItem Header="Cu_t" Click="MenuEditCut_Click" InputGestureText="Ctrl+X"/>
                <MenuItem Header="_Copy" Click="MenuEditCopy_Click" InputGestureText="Ctrl+C"/>
                <MenuItem Header="_Paste" Click="MenuEditPaste_Click" InputGestureText="Ctrl+V"/>
                <Separator/>
                <MenuItem Header="_Find" Click="MenuEditFind_Click" InputGestureText="Ctrl+F"/>
                <MenuItem Header="Find _Next" Click="MenuEditFindNext_Click" InputGestureText="F3"/>
                <MenuItem Header="Find _Previous" Click="MenuEditFindPrevious_Click" InputGestureText="Shift+F3"/>
                <MenuItem Header="_Replace" Click="MenuEditReplace_Click" InputGestureText="Ctrl+H"/>
                <Separator/>
                <MenuItem Header="Select _All" Click="MenuEditSelectAll_Click" InputGestureText="Ctrl+A"/>
            </MenuItem>

            <!-- View Menu -->
            <MenuItem Header="_View">
                <MenuItem Header="_Word Wrap" IsCheckable="True" Click="MenuViewWordWrap_Click"/>
                <MenuItem Header="_Dark Mode" x:Name="MenuViewDarkMode" IsCheckable="True" Click="MenuViewDarkMode_Click"/>
            </MenuItem>

            <!-- Help Menu -->
            <MenuItem Header="_Help">
                <MenuItem Header="_Help Topics" Click="MenuHelpTopics_Click" InputGestureText="F1"/>
                <Separator/>
                <MenuItem Header="_About SmallNotePad" Click="MenuHelpAbout_Click"/>
            </MenuItem>
        </Menu>

        <!-- Text Editor -->
        <TextBox x:Name="TextEditor" 
                 DockPanel.Dock="Bottom"
                 TextChanged="TextEditor_TextChanged"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 FontFamily="Consolas"
                 FontSize="12"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"/>
    </DockPanel>
</Window>

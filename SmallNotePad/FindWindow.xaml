<Window x:Class="SmallNotePad.FindWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find" Height="120" Width="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <StackPanel Margin="10">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Find what:" VerticalAlignment="Center" Width="80"/>
            <TextBox x:Name="SearchTermTextBox" Width="280" VerticalAlignment="Center" Padding="5"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button x:Name="FindButton" Content="Find All" Click="FindButton_Click" Width="70" Margin="0,0,5,0"/>
            <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Width="70"/>
        </StackPanel>
    </StackPanel>
</Window>
